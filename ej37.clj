; 37. Definir la función pos-inicial que, dadas dos listas simples, devuelva la posición inicial en la cual
; la primera lista se encuentra contenida en la segunda. Si no se encuentra contenida, la función devolverá cero. 
; Por ejemplo:
;     (pos-inicial '(C D) '(A B C D E F)) → 3
;     (pos-inicial '(A C) '(A B C D E F)) → 0
