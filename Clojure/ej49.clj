;49. Definir las funciones ieee-754 que reciba un número y devuelva una lista con los 32 bits correspondientes a su representación
; en notación de punto flotante según la norma IEEE-754 (single) y bin2hex que reciba una lista de bits
; y devuelva una cadena con su representación en hexadecimal.
